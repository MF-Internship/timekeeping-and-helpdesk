# US4 Config verification

Verified 2026-08-18: singleton reads/updates, Manager-only mutation, RBAC-before-DTO precedence,
same-value no-op, finite/positive meter validation, ordering invariants, and maximum-radius cap
against active and inactive Locations pass in the backend gate.
